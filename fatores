fatores<-function(x){
	nc<-ncol(x)
	c<-integer()
	cols<-integer()
	k<-1
	i<-1
	for(i in 1:nc){
		cols[i]<-x[1,i]
	}
	for(i in 1:nc){
		if(class(x[1,i])=="factor"){
		c[k]<-i
		k<-k+1
		}
	}
	return(c)
}
